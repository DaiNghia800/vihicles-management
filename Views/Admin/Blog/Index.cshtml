@{
    Layout = "_LayoutAdmin";
    ViewData["title"] = "Blog Management";
}

<style>
    .blog-management {
        padding: 1.25rem;
    }

    .header-section {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 1.875rem;
    }

    .page-title {
        font-size: 1.75rem;
        font-weight: 600;
        color: #333;
        margin: 0;
    }

    .header-actions {
        display: flex;
        gap: 0.9375rem;
        align-items: center;
    }

    .search-box {
        position: relative;
    }

        .search-box input {
            padding: 0.625rem 2.5rem 0.625rem 0.9375rem;
            border: 0.0625rem solid #ddd;
            border-radius: 0.5rem;
            width: 18.75rem;
            font-size: 0.875rem;
            transition: all 0.3s;
        }

            .search-box input:focus {
                outline: none;
                border-color: #14a38b;
                box-shadow: 0 0 0 0.1875rem rgba(20, 163, 139, 0.1);
            }

    .search-icon {
        position: absolute;
        right: 0.9375rem;
        top: 50%;
        transform: translateY(-50%);
        color: #999;
    }

    .btn {
        padding: 0.625rem 1.25rem;
        border: none;
        border-radius: 0.5rem;
        cursor: pointer;
        font-size: 0.875rem;
        font-weight: 500;
        transition: all 0.3s;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
    }

    .btn-primary {
        background-color: #14a38b;
        color: white;
    }

        .btn-primary:hover {
            background-color: #0f8570;
            transform: translateY(-0.0625rem);
            box-shadow: 0 0.25rem 0.75rem rgba(20, 163, 139, 0.3);
        }

    /* Bỏ styling cũ của .btn-edit và .btn-danger vì chúng ta dùng icon */
    /*
        .btn-edit { ... }
        .btn-danger { ... }
        */

    .table-card {
        background: white;
        border-radius: 0.75rem;
        /* padding: 1.5625rem; */
        box-shadow: 0 0.125rem 0.5rem rgba(0,0,0,0.08);
    }

    .blog-table {
        width: 100%;
        border-collapse: collapse;
    }

        .blog-table thead {
            background-color: #f8f9fa;
        }

        .blog-table th {
            padding: 0.9375rem;
            text-align: left;
            font-weight: 600;
            color: #000000;
            border-bottom: 0.125rem solid #dee2e6;
            font-size: 0.875rem;
            text-transform: uppercase;
            letter-spacing: 0.03125rem;
        }

        .blog-table td {
            padding: 0.9375rem;
            border-bottom: 0.0625rem solid #f0f0f0;
            vertical-align: middle;
        }

        .blog-table tbody tr {
            transition: all 0.3s;
        }

            .blog-table tbody tr:hover {
                background-color: #f8f9fa;
                /* transform: scale(1.01); - Bỏ để tránh giật lag khi hover */
            }

    .blog-image {
        width: 5rem;
        height: 3.75rem;
        object-fit: cover;
        border-radius: 0.5rem;
        box-shadow: 0 0.125rem 0.25rem rgba(0,0,0,0.1);
    }

    .no-image {
        width: 5rem;
        height: 3.75rem;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        border-radius: 0.5rem;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-size: 0.75rem;
        font-weight: 500;
    }

    .blog-title {
        max-width: 21.875rem;
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
        font-weight: 500;
        color: #333;
        font-size: 0.9375rem;
    }

        .blog-title:hover {
            color: #14a38b;
            cursor: pointer;
        }

    .category-badge {
        display: inline-block;
        padding: 0.375rem 0.875rem;
        background-color: #e3f2fd;
        color: #1976d2;
        border-radius: 1.25rem;
        font-size: 0.8125rem;
        font-weight: 500;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
        max-width: 100%;
    }

    .author-info {
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    .author-avatar {
        width: 2rem;
        height: 2rem;
        border-radius: 50%;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-size: 0.75rem;
        font-weight: 600;
    }

    .author-name {
        font-size: 0.875rem;
        color: #555;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
        max-width: 100%;
    }

    .date-text {
        color: #777;
        font-size: 0.8125rem;
    }

    .action-buttons {
        display: flex;
        /* gap: 0.625rem; /* Tăng khoảng cách */ */
    }

    /* === BẮT ĐẦU CSS CHO ICON === */
    .action-icon {
        display: inline-block;
        width: 2.25rem;
        height: 2.25rem;
        padding: 0.5rem; /* Giữ padding để vùng click lớn hơn */
        border: none;
        border-radius: 50%; /* Luôn là hình tròn */
        background-color: transparent; /* Nền trong suốt */
        text-decoration: none;
        cursor: pointer;
        transition: all 0.2s ease-in-out;
        /* Bỏ 'color: white;' - màu sẽ được set riêng */
    }

        .action-icon svg {
            width: 100%;
            height: 100%;
        }

    .icon-view {
        color: #747dc6; /* Màu icon là màu xanh */
    }

        .icon-view:hover {
            background-color: rgba(20, 163, 139, 0.0005);
        }

    .icon-edit {
        color: #4aa4d9; /* Màu icon là màu vàng */
    }

        .icon-edit:hover {
            background-color: rgba(255, 193, 7, 0.0005);
        }

    .icon-delete {
        color: #dc3545; /* Màu icon là màu đỏ */
    }

        .icon-delete:hover {
            background-color: rgba(220, 53, 69, 0.0005); 
        }

        
    /* === KẾT THÚC CSS CHO ICON === */

    .no-data {
        text-align: center;
        padding: 3.75rem 1.25rem;
        color: #999;
    }

    .no-data-icon {
        font-size: 4rem;
        margin-bottom: 1.25rem;
        opacity: 0.3;
    }

    .no-data-text {
        font-size: 1.125rem;
        font-weight: 500;
        margin-bottom: 0.625rem;
    }

    .loading {
        text-align: center;
        padding: 2.5rem;
        color: #14a38b;
    }

    .spinner {
        border: 0.1875rem solid #f3f3f3;
        border-top: 0.1875rem solid #14a38b;
        border-radius: 50%;
        width: 2.5rem;
        height: 2.5rem;
        animation: spin 1s linear infinite;
        margin: 0 auto 0.9375rem;
    }

    @@keyframes spin {
        0% {
            transform: rotate(0deg);
        }

        100% {
            transform: rotate(360deg);
        }
    }
</style>

<div class="blog-management">
    <div class="header-section">
        <h1 class="page-title">Blog Management</h1>
        <div class="header-actions">
            <div class="search-box">
                <input type="text" id="searchInput" placeholder="Search blogs..." />
                @* <span class="search-icon">🔍</span> *@
            </div>
            <a href="/admin/blog/create" class="btn btn-primary">
                Add New Blog
            </a>
        </div>
    </div>

    <div class="table-card">
        <div id="loadingIndicator" class="loading" style="display: none;">
            <div class="spinner"></div>
            <p>Loading blogs...</p>
        </div>

        <table class="blog-table" id="blogTable">
            <thead>
                <tr>
                    <th>Image</th>
                    <th>Title</th>
                    <th>Category</th>
                    <th>Author</th>
                    <th>Created Date</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody id="blogTableBody">
                <!-- Data will be loaded here -->
            </tbody>
        </table>
    </div>
</div>

<script>
    let blogs = [];
    let categories = [];
    let authors = [];

    document.addEventListener('DOMContentLoaded', function () {
        loadData();
        setupSearch();
    });

    async function loadData() {
        showLoading(true);
        try {
            // Load categories và authors TRƯỚC
            await Promise.all([
            loadCategories(),
            loadAuthors()
            ]);

            // Load blogs SAU KHI categories và authors đã có
            await loadBlogs();
        } catch (error) {
            console.error('Error loading data:', error);
            showNoData();
        } finally {
            showLoading(false);
        }
    }

    function showLoading(show) {
        document.getElementById('loadingIndicator').style.display = show ? 'block' : 'none';
        document.getElementById('blogTable').style.display = show ? 'none' : 'table';
    }

    async function loadBlogs() {
        try {
            const response = await fetch('/admin/blog/api/list');
            if (response.ok) {
                blogs = await response.json();
                renderTable(); // Lúc này categories và authors đã sẵn sàng
            } else {
                console.error('Error loading blogs');
                showNoData();
            }
        } catch (error) {
            console.error('Error:', error);
            showNoData();
        }
    }

    async function loadCategories() {
        try {
            const response = await fetch('/api/BlogCategories'); // Sửa lại đường dẫn API
            if (response.ok) {
                categories = await response.json();
            }
        } catch (error) {
            console.error('Error loading categories:', error);
        }
    }

    async function loadAuthors() {
        try {
            const response = await fetch('/admin/blog/api/authors');
            if (response.ok) {
                authors = await response.json();
            }
        } catch (error) {
            console.error('Error loading authors:', error);
        }
    }

    function renderTable(data = blogs) {
        const tbody = document.getElementById('blogTableBody');

        if (data.length === 0) {
            showNoData();
            return;
        }

        tbody.innerHTML = data.map(blog => `
            <tr>
                <td>
                    ${blog.imageUrl ?
            `<img src="${blog.imageUrl}" alt="${blog.title}" class="blog-image" />` :
            '<div class="no-image">No Image</div>'}
                </td>
                <td>
                    <div class="blog-title" title="${blog.title}">
                        ${blog.title}
                    </div>
                </td>
                <td>
                    <span class="category-badge">${getCategoryName(blog.categoryUid)}</span>
                </td>
                <td>
                    <div class="author-info">
                        <div class="author-avatar">${getInitials(getAuthorName(blog.authorUid))}</div>
                        <span class="author-name">${getAuthorName(blog.authorUid)}</span>
                    </div>
                </td>
                <td>
                    <span class="date-text">${formatDate(blog.createdAt)}</span>
                </td>
                <td>
                    <!-- === BẮT ĐẦU THAY THẾ NÚT === -->
                    <div class="action-buttons">
                        <a href="/blog/${blog.uid}" target="_blank" class="action-icon icon-view" title="View Blog Post">
                            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor"><path d="M12.0003 3C17.3924 3 21.8784 6.87976 22.8189 12C21.8784 17.1202 17.3924 21 12.0003 21C6.60812 21 2.12215 17.1202 1.18164 12C2.12215 6.87976 6.60812 3 12.0003 3ZM12.0003 19C16.2359 19 19.8603 16.052 20.7777 12C19.8603 7.94803 16.2359 5 12.0003 5C7.7646 5 4.14022 7.94803 3.22278 12C4.14022 16.052 7.7646 19 12.0003 19ZM12.0003 16.5C9.51498 16.5 7.50026 14.4853 7.50026 12C7.50026 9.51472 9.51498 7.5 12.0003 7.5C14.4855 7.5 16.5003 9.51472 16.5003 12C16.5003 14.4853 14.4855 16.5 12.0003 16.5ZM12.0003 14.5C13.381 14.5 14.5003 13.3807 14.5003 12C14.5003 10.6193 13.381 9.5 12.0003 9.5C10.6196 9.5 9.50026 10.6193 9.50026 12C9.50026 13.3807 10.6196 14.5 12.0003 14.5Z"></path></svg>
                        </a>
                        <a href="/admin/blog/edit/${blog.uid}" class="action-icon icon-edit" title="Edit Blog Post">
                            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor"><path d="M15.7279 9.57627L14.3137 8.16206L5 17.4758V18.89H6.41421L15.7279 9.57627ZM17.1421 8.16206L18.5563 6.74785L17.1421 5.33363L15.7279 6.74785L17.1421 8.16206ZM7.24264 20.89H3V16.6473L16.435 3.21231C16.8256 2.82179 17.4587 2.82179 17.8492 3.21231L20.6777 6.04074C21.0682 6.43126 21.0682 7.06443 20.6777 7.45495L7.24264 20.89Z"></path></svg>
                        </a>
                        <button type="button" class="action-icon icon-delete" onclick="deleteBlog(${blog.uid})" title="Delete Blog Post">
                            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor"><path d="M7 4V2H17V4H22V6H20V21C20 21.5523 19.5523 22 19 22H5C4.44772 22 4 21.5523 4 21V6H2V4H7ZM6 6V20H18V6H6ZM9 9H11V17H9V9ZM13 9H15V17H13V9Z"></path></svg>
                        </button>
                    </div>
                    <!-- === KẾT THÚC THAY THẾ NÚT === -->
                </td>
            </tr>
        `).join('');
    }

    function showNoData() {
        const tbody = document.getElementById('blogTableBody');
        tbody.innerHTML = `
            <tr>
                <td colspan="6" class="no-data">
                    <div class="no-data-icon">📝</div>
                    <div class="no-data-text">No blogs found</div>
                    <p>Start by creating your first blog post!</p>
                </td>
            </tr>
        `;
    }

    function getCategoryName(uid) {
        // Kiểm tra nếu categories rỗng hoặc không
        if (!categories || categories.length === 0) return 'Unknown';
        const category = categories.find(c => c.uid === uid);
        return category ? category.name : 'Unknown';
    }

    function getAuthorName(uid) {
        // Kiểm tra nếu authors rỗng hoặc không
        if (!authors || authors.length === 0) return 'Unknown';
        const author = authors.find(a => a.uid === uid);
        return author ? author.fullName : 'Unknown';
    }

    function getInitials(name) {
        if (!name || name === 'Unknown') return '??';
        return name.split(' ').map(n => n[0]).join('').toUpperCase().substring(0, 2);
    }

    function formatDate(dateString) {
        const date = new Date(dateString);
        return date.toLocaleDateString('vi-VN', {
            day: '2-digit',
            month: '2-digit',
            year: 'numeric'
        });
    }

    async function deleteBlog(uid) {
        if (!confirm('Are you sure you want to delete this blog? This action cannot be undone.')) return;

        try {
            const response = await fetch(`/admin/blog/api/delete/${uid}`, {
                method: 'DELETE'
            });

            if (response.ok) {
                alert('Blog deleted successfully!');
                // Tải lại chỉ blogs, không cần tải lại categories/authors
                loadBlogs();
            } else {
                const error = await response.json();
                alert('Error: ' + (error.message || 'Could not delete blog'));
            }
        } catch (error) {
            console.error('Error:', error);
            alert('Error deleting blog');
        }
    }

    function setupSearch() {
        document.getElementById('searchInput').addEventListener('input', function (e) {
            const searchTerm = e.target.value.toLowerCase();
            const filtered = blogs.filter(blog =>
                blog.title.toLowerCase().includes(searchTerm) ||
                getCategoryName(blog.categoryUid).toLowerCase().includes(searchTerm) ||
                getAuthorName(blog.authorUid).toLowerCase().includes(searchTerm)
            );
            renderTable(filtered);
        });
    }
</script>